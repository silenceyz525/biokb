# 2026-04-09 工作记录

## BioKB 每日采集任务

- 执行时间：08:59
- 采集结果：共采集 149 篇，新增 110 篇，跳过 39 篇重复
- 数据库总量：774 篇（+110，上次 664 篇）
- 导出：data/articles.json 已更新
- PubMed 精准医疗查询本次超时（其余 PubMed API 查询正常）

## 页面显示修复（16:30）

**问题**：GitHub Pages 上所有板块统计卡片均显示 0
**根因**：`loadScholars()` 和 `loadPhageTrials()` 在页面初始化时未调用，学者 Tab 统计 count-scholar 也从未更新

**修复内容**：
- `loadScholars()` 加入页面初始化（Init 段），学者Tab预加载
- 新增 `updateScholarCount()` 函数，在 `renderScholars()` 内部调用以更新学者数量
- 推送 commit: `fb46104`

## 噬菌体临床试验板块开发

- 新增 `scripts/phage_trials.py` 数据采集脚本
- 更新 `server.py` 添加 `/api/phage-trials` API 端点
- 更新 `index.html` 添加噬菌体临床试验展示页面
- 更新 `push-to-github.py` 添加自动采集步骤
- 成功采集 73 项临床试验数据
- 页面包含：统计卡片、图表、病原体标签、变化追踪、可筛选表格
- 自动化任务已更新，每日 8:00 自动采集
